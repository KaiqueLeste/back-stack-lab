apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: crossview-server-route
  namespace: crossview
spec:
  parentRefs:
  - name: shared-gateway
    namespace: gateway-infrastructure
  hostnames:
  - crossview.kleste.lab
  rules:
  - backendRefs:
    - name: crossview-service
      port: 80
